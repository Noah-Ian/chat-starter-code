# WebSockets-ws Chat Application

This is a simple Chat Application that uses web socket technology to enable commmunication between a Client and a Server

To run this application, first open up a terminal. Then, run the command below:

```sh
npm install
node server.js
```
